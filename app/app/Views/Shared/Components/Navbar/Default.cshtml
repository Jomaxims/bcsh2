@using app.Managers

<nav class="navbar navbar-expand-lg shadow-sm">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="Index">Domů</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Zajezdy" asp-action="Zajezdy">Zajezdy</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-controller="Home" asp-action="Kontakt">Kontakt</a>
                </li>
                @if (User.IsInRole(Role.Zamestnanec))
                {
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Sprava" asp-action="Polozky">Správa</a>
                    </li>
                }
                @if (User.IsInRole(Role.Admin))
                {
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Admin" asp-action="Panel">Admin panel</a>
                    </li>
                }
            </ul>
            <div class="d-flex">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    @if (User.Identity!.IsAuthenticated)
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Logout">Odhlásit</a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Login">Přihlásit</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Register">Registrace</a>
                        </li>
                    }
                    @if (User.IsInRole(Role.Zakaznik))
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Profil" asp-action="Profil">Profil</a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </div>
</nav>

