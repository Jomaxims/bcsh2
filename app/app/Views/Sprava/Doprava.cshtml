@model IEnumerable<app.Models.Sprava.DopravaModel>

@{
    ViewBag.Title = "Dopravy";
}

<div class="container-md mt-3">
    <table class="table table-striped">
        <thead>
        <tr>
            <th scope="col">Název</th>
            <th scope="col"><a class="btn btn-primary" asp-action="DopravaEdit" asp-route-id="0">Přidat</a></th>
        </tr>
        </thead>
        <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nazev</td>
                <td>
                    <a asp-action="DopravaEdit" asp-route-id="@item.DopravaId">Upravit</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>